'use client';

// Seller settings uses the same component as buyer settings
import BuyerSettings from '@/app/buyer/settings/page';

export default function SellerSettings() {
  return <BuyerSettings />;
}
